{
  "build": {
    "command": "composer install --no-dev && php artisan optimize"
  },
  "deploy": {
    "startCommand": "bash railway-startup.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
